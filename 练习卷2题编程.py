startpos=int(input("the array start position"))
NumToChange= int(input("the number of elemnets to change"))
n=0
while n !=NumToChange-1:
    NewChar=input("input a new substitute character")
    Lookup [Startpos+n]=NewChar,
    n+=1
print(NumToChange, "elements have been changed")
